services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    ports: ["27017:27017"]
    volumes: [mongo_data:/data/db]
  api:
    build: ./backend
    command: npm run dev
    env_file: ./backend/.env
    ports: ["5000:5000"]
    depends_on: [mongo]
volumes:
  mongo_data:
